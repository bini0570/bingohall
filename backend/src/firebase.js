// Firebase has been removed. This project now uses Supabase exclusively.
// This stub prevents import errors if any old code still references this file.

function initFirebase() { return null; }
function isFirebaseConfigured() { return false; }
function getFirestoreDB() { return null; }

module.exports = { initFirebase, isFirebaseConfigured, getFirestoreDB };
